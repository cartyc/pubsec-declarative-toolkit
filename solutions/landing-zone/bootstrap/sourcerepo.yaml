apiVersion: sourcerepo.cnrm.cloud.google.com/v1beta1
kind: SourceRepoRepository
metadata:
  name: landing-zone
  annotations:
    cnrm.cloud.google.com/project-id: ${management-project-id} # kpt-set: ${management-project-id}
  labels:
    solution: landing-zone-bootstrap